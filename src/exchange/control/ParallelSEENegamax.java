package exchange.control;

class ParallelSEENegamax {
/**
 * is there a way to do this without resorting to using gain[] which in this case would have to be an array of parallel arithmetic vectors?
 * if gain[] must be used, how wide does it need to be? Can i assume the value in it will never exceed value(QUEEN) when the final gain/loss is reported?
 */
}
